# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — Concrete Anomaly Inspector (HF Spaces deployment)
# Generated from local env (concrete_pfe) + app.py imports
# ──────────────────────────────────────────────────────────────────────────────

# ── Core ML / DL ──────────────────────────────────────────────────────────────
torch==2.11.0
torchvision==0.26.0

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python-headless==4.13.0.92   # headless variant required on HF Spaces (no display)
pillow==11.3.0

# ── Numerical / Scientific ────────────────────────────────────────────────────
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2

# ── Persistence / Serialisation ───────────────────────────────────────────────
joblib==1.5.3                        # load kmeans_stages.pkl / kmeans_scaler.pkl

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib==3.10.8

# ── Web UI ────────────────────────────────────────────────────────────────────
gradio                               # let HF resolve the latest compatible version

# ── Config / Environment ──────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Roboflow / Inference (optional — graceful fallback if absent) ─────────────
inference==1.2.3
inference-sdk==1.2.6

# ──────────────────────────────────────────────────────────────────────────────
# Notes
# ──────────────────────────────────────────────────────────────────────────────
# • opencv-python-headless replaces opencv-python and opencv-contrib-python;
#   both conflict on HF Spaces (no X server). Your app uses only core cv2 ops
#   (resize, addWeighted, findContours, ellipse, boundingRect) — all present.
#
# • gradio is intentionally left unpinned: HF Spaces pre-installs a version and
#   pinning can cause solver conflicts. If you need a specific version add e.g.
#   gradio==5.x.x after testing.
#
# • torch 2.11.0 ships its own CUDA/CPU dispatch; no separate torchvision CUDA
#   index needed for CPU-only Spaces (free tier). For GPU Spaces add:
#     --extra-index-url https://download.pytorch.org/whl/cu121
#   before torch/torchvision lines.
#
# • segmentation_models_pytorch (smp) is NOT imported in app.py — the U-Net is
#   defined inline — so it is intentionally excluded.
#
# • inference pulls supervision, requests, pydantic automatically; no need to
#   list them separately.