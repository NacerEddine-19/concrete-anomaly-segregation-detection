# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — Concrete Anomaly Inspector (HF Spaces deployment)
# Adapted directly from your local operational environment (concrete_pfe)
# ──────────────────────────────────────────────────────────────────────────────

# ── Core ML / DL (CPU-optimized index mapping to prevent image space timeout crashes) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
torchvision==0.26.0+cpu

# ── Web Application Dashboard Layout ──────────────────────────────────────────
gradio==6.15.1

# ── Computer Vision & Roboflow Pipeline Suite ────────────────────────────────
inference==1.2.3
inference-sdk==1.2.6
ultralytics==8.3.40
supervision==0.27.0.post2
opencv-python-headless==4.13.0.92   # Replaces desktop variants to satisfy missing server displays
pillow==11.3.0

# ── Numerical / Analytics Framework ───────────────────────────────────────────
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2                 # Fixed to completely eliminate KMeans unpickling mismatches

# ── Persistence, Visual Cards & Environment Configuration ──────────────────────
joblib==1.5.3                       # Match local serialization component version
matplotlib==3.10.8                  # Match local inspection card visualization rendering engine
python-dotenv==1.0.1
timm==1.0.26